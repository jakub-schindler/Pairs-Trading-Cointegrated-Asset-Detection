[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pairs-trading"
version = "0.1.0"
description = "Pairs trading analysis tools"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy",
    "yfinance",
]
